import React from "react";
import { SafeAreaView } from "react-native";


export default LoginScreen ({ navigation }: { navigation?: any })
{
    return(
        
        <SafeAreaView>
            <view>
                <Text>Authentication Page</Text>
            </view>
        </SafeAreaView>
    )
}